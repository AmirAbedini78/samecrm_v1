# تحلیل جامع بخش حسابداری و راهکارهای هوش مصنوعی / یادگیری ماشین

این سند نتیجه بررسی فایل‌ها، دیتابیس و ماژول‌های حسابداری (انبار، انبار بلزونا، فروش، فاکتور، تسویه، گزارشات) است و فیچرهای پیشنهادی با ML/DL/AI و الزامات داده را پوشش می‌دهد.

---

## ۱. خلاصه بررسی فایل‌ها و دیتابیس

### ۱.۱ جداول و ساختار فعلی

| ماژول | جدول(ها) | فیلدهای کلیدی |
|--------|----------|----------------|
| **انبار (Inventory)** | `inventory` | inventory_id, inventory_code, inventory_name, first_period_*, input_*, output_*, current_quantity/amount/avg_price, minimum_stock, main_unit, warehouse (از migration بعدی), physical_available |
| **ورود انبار (گردش قلمی)** | `inventory_entries` | entry_id, inventory_id, entry_date, entry_code, entry_type, document_number, quantity, unit_price, total_amount, import_batch |
| **تراکنش انبار** | `inventory_transactions` | transaction_id, inventory_id, transaction_type, quantity, amount, transaction_date, document_number, warehouse, user_id |
| **انبار بلزونا** | `belzona_inventories` | product_name, date, input, output, balance, invoice_number, customer_name, sheet_name, sheet_row_number, product_weight_* |
| **فروش** | `sales` | document_number, document_date, customer_code/name, product_code/name/barcode, main_quantity, base_price, base_sales_amount, base_net_amount, warehouse, month, issued_*, remaining_*, sales_status |
| **تسویه فاکتور** | `invoice_settlements` | document_number, document_date, customer_name, base_net_amount, paid_amount, balance_amount, currency |
| **حسابداری عمومی** | `accounting` | accounting_title, amount, date, type, payment_status, reference |

گزارشات انبار از `InventoryReportService` و `InventoryReportRepository` استفاده می‌کنند و داده‌ها را از همین جداول می‌خوانند.

### ۱.۲ وضعیت فعلی «ایمپورت گردش کالا» و PDF

- **وضعیت:** در بخش گزارش انبار، ایمپورت ورود انبار فقط **فایل Excel/CSV** را می‌پذیرد و **کد کالا (inventory_code) به صورت دستی توسط کاربر** وارد می‌شود.
- **محدودیت:** پشتیبانی از **PDF وجود ندارد**. اگر قرار باشد فایل PDF گردش کالا آپلود شود، باید:
  1. **استخراج متن/جدول از PDF** (OCR یا متن‌یابی در PDF).
  2. **تشخیص خودکار محصول** از روی محتوای PDF (نام/کد کالا در سند).
  3. **نگاشت ردیف‌های استخراج‌شده** به فیلدهای `inventory_entries` (تاریخ، سند، نوع، مقدار، فی، مبلغ و غیره).

یعنی فیچر «خواندن PDF و تشخیص خودکار محصول برای ایمپورت گردش کالا» در کد فعلی **پیاده نشده** است و باید از صفر طراحی و پیاده شود (ترجیحاً با سرویس Python برای OCR/NER و API برای Laravel).

---

## ۲. فیچر پیشنهادی: خواندن PDF گردش کالا و تشخیص خودکار محصول

### هدف
کاربر فقط فایل PDF گردش کالا را آپلود کند؛ سیستم خودش تشخیص دهد مربوط به **کدام محصول (کد/نام)** است و ردیف‌های ورود/خروج را استخراج و در `inventory_entries` ذخیره کند.

### معماری پیشنهادی

1. **Backend (Laravel):**
   - آپلود PDF و ذخیره موقت.
   - فراخوانی سرویس Python (مثلاً REST یا صف job) با مسیر فایل.
   - دریافت خروجی شامل: `inventory_code` (یا نام محصول + تطبیق با `inventory`)، لیست ردیف‌ها (تاریخ، سند، نوع، مقدار، فی، مبلغ).
   - اعتبارسنجی و ذخیره در `inventory_entries` از طریق `InventoryEntryService` و در نهایت به‌روزرسانی موجودی.

2. **سرویس Python:**
   - **استخراج متن/جدول از PDF:** کتابخانه‌هایی مثل `pdfplumber`, `PyMuPDF`, یا `camelot` برای جداول.
   - **OCR در صورت اسکن/تصویر:** `pytesseract` + پیش‌پردازش تصویر یا سرویس ابری OCR در صورت نیاز.
   - **تشخیص محصول:**
     - جستجوی الگو برای **کد کالا** (مثلاً الگوی عددی/حروفی مشخص).
     - یا **NER (Named Entity)** روی نام محصول با مدل زبانی سبک یا قانون مبتنی بر کلمات کلیدی.
     - یا **تطبیق با لیست کالاهای سیستم:** ارسال لیست `inventory_code` / `inventory_name` به Python و پیدا کردن بهترین تطابق در متن PDF (فازی یا embedding).
   - خروجی ساخت‌یافته JSON برای Laravel.

3. **تکمیل داده برای دقت بهتر:**
   - در دیتابیس: نام و کد یکسان و بدون نویز در `inventory` (تا تطبیق با متن PDF قابل اعتماد باشد).
   - در صورت امکان: نمونه PDFهای واقعی با برچسب «این PDF مربوط به کد X است» برای آموزش/تنظیم قانون یا مدل کوچک.

این فیچر را می‌توان به‌صورت تدریجی پیاده کرد: اول فقط استخراج جدول از PDF + یک فیلد «کد کالا» دستی؛ بعد اضافه کردن تشخیص خودکار محصول با قوانین یا مدل ساده.

---

## ۳. فیچرهای قابل پیاده‌سازی با داده فعلی

با همان جداول و داده‌ای که الان دارید (بدون تغییر اساسی اسکیما)، این فیچرها قابل طراحی هستند:

### ۳.۱ تحلیل و گزارش (بدون ML سنگین)

- **گزارش مقایسه دو بازه فروش** (قبلاً هست) و گسترش آن با فیلتر محصول/مشتری/انبار.
- **گزارش موجودی فعلی، انقضا، خارج از انبار، تراکنش‌ها و ورودهای انبار** (موجود).
- **آنالیز درصد مشتریان (پارتو/ABC)** روی فروش (موجود در SalesReports).
- **روند ماهانه فروش، محصولات پرفروش، مشتریان برتر** (موجود).

این‌ها پایه‌ی داشبورد تحلیلی هستند و برای «فاز تحلیل و پیش‌بینی» لازم‌اند.

### ۳.۲ یادگیری ماشین کلاسیک (با داده فعلی)

- **پیش‌بینی تقاضا (Demand Forecasting) برای هر محصول/انبار:**
  - ورودی: سری زمانی `main_quantity` یا `base_sales_amount` از `sales` (مثلاً تجمع هفتگی/ماهانه).
  - مدل: ARIMA، Exponential Smoothing، یا رگرسیون با لگ‌های زمانی در Python (مثلاً با `statsmodels`, `scikit-learn`).
  - خروجی: پیش‌بینی مقدار/مبلغ برای هفته یا ماه بعد (برای برنامه‌ریزی سفارش/موجودی).

- **هشدار موجودی کم (قوی‌تر از حد ساده):**
  - با توجه به نرخ خروج و پیش‌بینی تقاضا، «زمان رسیدن به حداقل موجودی» یا «مقدار پیشنهادی سفارش» (قابل پیاده با قوانین ساده یا مدل رگرسیون).

- **خوشه‌بندی مشتریان (بدون برچسب):**
  - از `sales`: مجموع خرید، تعداد فاکتور، تنوع محصول، آخرین خرید.
  - K-Means یا Hierarchical روی این ویژگی‌ها برای segment بندی (A/B/C یا بازاریابی هدفمند).

- **تشخیص ناهنجاری (Anomaly Detection):**
  - روی مبلغ یا تعداد فاکتور روزانه/هفتگی: مدل Isolation Forest یا مشابه برای تشخیص روزهای غیرعادی (تقلب یا خطای ورود).

این موارد با داده‌ی خام فعلی (فروش، موجودی، تسویه) قابل انجام هستند؛ کیفیت به تمیز بودن و طول تاریخچه داده بستگی دارد.

### ۳.۳ یادگیری عمیق (با داده فعلی، در حد محدود)

- **پیش‌بینی سری زمانی با LSTM/Transformer:**
  - همان سری‌های فروش یا موجودی؛ نیاز به داده کافی (ترجیحاً ماه‌ها/سال‌ها) و نرمال‌سازی.
- **تشخیص ناهنجاری با Autoencoder** روی بردار ویژگی‌های روزانه/هفتگی (فروش، تعداد تراکنش و غیره).

نکته: با داده کم، مدل‌های ساده (ARIMA، رگرسیون) معمولاً پایدارتر از شبکه عمیق هستند.

---

## ۴. فیچرهایی که با «تکمیل اطلاعات» امکان‌پذیر می‌شوند

با تکمیل و یکپارچه‌سازی داده، این قابلیت‌ها قابل اضافه شدن هستند:

### ۴.۱ لینک فروش ↔ انبار ↔ تسویه

- **اگر** هر ردیف فروش (`sales`) به `inventory_id` یا حداقل `product_code` یکسان با `inventory.inventory_code` متصل باشد و خروج انبار از همان منبع ثبت شود، می‌توان:
  - **سود ناخالص به ازای محصول:** مقایسه قیمت فروش با «میانگین قیمت تمام‌شده» از `inventory_entries` / موجودی.
  - **تحلیل حاشیه سود و ترکیب سبد فروش** با ML (خوشه‌بندی، پیش‌بینی سود).

### ۴.۲ تاریخ و وضعیت تحویل

- اگر فیلدهای **تاریخ تحویل**، **وضعیت تحویل** (ارسال شده / در راه / تحویل شده) و در صورت امکان **منطقه/شهر** در فروش یا جدول مرتبط وجود داشته باشد:
  - **پیش‌بینی زمان تحویل** (مثلاً با رگرسیون یا مدل ساده).
  - **تحلیل تأخیر و شکایت** (اگر داده شکایت هم اضافه شود).

### ۴.۳ دلیل برگشت / لغو

- اگر **دلیل برگشت یا لغو** و **نوع فاکتور** (عادی/برگشتی) در داده باشد:
  - **تحلیل علت برگشت** (طبقه‌بندی متنی یا برچسب‌های از پیش‌تعریف‌شده).
  - **پیش‌بینی احتمال برگشت** به ازای محصول/مشتری (مدل طبقه‌بندی).

### ۴.۴ قیمت تمام‌شده و هزینه

- اگر **قیمت تمام‌شده یا هزینه هر واحد** در سطح ردیف فروش یا در سطح محصول-زمان ذخیره شود:
  - **تحلیل سود واقعی و حاشیه سود** به ازای محصول، مشتری، دوره.
  - **پیش‌بینی سود و جریان نقدی** با ML.

### ۴.۵ تسویه و اعتبار

- اگر **تاریخ سررسید**، **تاریخ پرداخت واقعی** و **نوع پرداخت** در `invoice_settlements` یا جدول مرتبط با فاکتور باشد:
  - **پیش‌بینی وصول** (چه زمانی پول وصول می‌شود).
  - **امتیاز اعتبار مشتری** و پیشنهاد حد اعتبار (قانونی یا با مدل ساده).

---

## ۵. دقیقاً چه داده‌هایی باید تکمیل شوند؟

برای بهره‌گیری حداکثری از ML/AI در بخش حسابداری، پیشنهاد می‌شود داده به شکل زیر تکمیل و یکپارچه شود:

### ۵.۱ شناسه‌های یکپارچه

- **ارتباط فروش با انبار:** در `sales` یا در یک جدول میانی، نگه‌داری `inventory_id` یا اطمینان از یکسان بودن `product_code` با `inventory.inventory_code` و استفاده مداوم از همان کد.
- **ارتباط تسویه با فاکتور/فروش:** در `invoice_settlements` یا جدول فاکتور، وجود `document_number` یکسان و در صورت امکان `sales_id` یا ارجاع به فاکتور برای پیوند با ردیف فروش.

### ۵.۲ فیلدهای زمانی و وضعیت

- **فروش:** در صورت امکان `delivery_date`, `delivery_status` (مثلاً enum: pending, shipped, delivered), و در صورت امکان `region`/`city` برای تحویل.
- **تسویه:** `due_date`, `paid_at` (تاریخ پرداخت واقعی), `payment_method` (نقد، چک، انتقال و غیره).

### ۵.۳ مالی و هزینه

- **قیمت تمام‌شده:** در سطح ردیف فروش (`sales`) یک فیلد مثل `unit_cost` یا `cost_of_goods_sold`، یا محاسبه از `inventory_entries`/موجودی با یک قانون ثابت (مثلاً FIFO) و ذخیره در یک view/جدول خلاصه برای تحلیل.
- **برگشت و لغو:** در `sales` یا جدول جدا: `is_return`, `return_reason`, `return_date`.

### ۵.۴ متن و برچسب برای NLP

- برای فیچرهایی مثل **تشخیص محصول از PDF** یا **دسته‌بندی دلیل برگشت:**
  - **نام و کد محصول یکسان و بدون نویز** در `inventory`.
  - در صورت امکان **نمونه PDFهای گردش کالا با برچسب** (این PDF مربوط به کد X است) برای آموزش/تست.
  - برای برگشت: **دلایل از پیش‌تعریف‌شده یا متن آزاد** و در صورت متن آزاد، بعداً برچسب‌گذاری برای آموزش مدل طبقه‌بندی.

برای **حجم و کیفیت دقیق داده** و **چک‌لیست تحویل به سازمان** سند جداگانه **[DATA-REQUIREMENTS-SMART-FEATURES.md](DATA-REQUIREMENTS-SMART-FEATURES.md)** را ببینید.

### ۵.۵ خلاصه جدول «داده مورد نیاز برای فیچر»

| فیچر پیشنهادی | داده فعلی کافی؟ | داده تکمیلی پیشنهادی |
|----------------|------------------|------------------------|
| پیش‌بینی تقاضا | بله (فروش زمانی) | تاریخچه طولانی‌تر، نویز کمتر |
| خوشه‌بندی مشتریان | بله | - |
| تشخیص ناهنجاری فروش | بله | - |
| خواندن PDF + تشخیص محصول | خیر | نمونه PDF با برچسب؛ نام/کد یکسان در inventory |
| تحلیل سود و حاشیه سود | تا حدی | unit_cost یا لینک به موجودی/ورود انبار |
| پیش‌بینی وصول | تا حدی | due_date, paid_at در تسویه |
| پیش‌بینی زمان تحویل | خیر | delivery_date, delivery_status |
| تحلیل/پیش‌بینی برگشت | خیر | is_return, return_reason, return_date |

---

## ۶. جمع‌بندی و اولویت‌بندی

- **وضعیت فعلی:** ایمپورت گردش کالا فقط Excel/CSV با **کد کالای دستی** است؛ **PDF و تشخیص خودکار محصول پیاده نشده** و باید به‌صورت سرویس جدا (ترجیحاً Python) طراحی شود.
- **با داده فعلی:** می‌توان پیش‌بینی تقاضا، خوشه‌بندی مشتریان، ناهنجاری و داشبورد تحلیلی را راه اندازی کرد.
- **با تکمیل داده (لینک فروش–انبار، تاریخ تحویل، تسویه با due/paid، هزینه و برگشت):** تحلیل سود، پیش‌بینی وصول، تحویل و برگشت با ML امکان‌پذیر می‌شوند.

پیشنهاد عملیاتی:
1. **کوتاه‌مدت:** طراحی سرویس PDF (استخراج جدول + تشخیص محصول) و اتصال به همان فلوی ایمپورت ورود انبار.
2. **میان‌مدت:** پیش‌بینی تقاضا و هشدار موجودی با مدل‌های کلاسیک و داشبورد تحلیلی یکپارچه.
3. **بلندمدت:** تکمیل فیلدهای پیشنهادی در دیتابیس و اضافه کردن تحلیل سود، وصول و در صورت نیاز مدل‌های عمیق‌تر برای سری زمانی یا متن.

اگر بخواهید، در مرحله بعد می‌توانیم برای یکی از این موارد (مثلاً طراحی دقیق سرویس PDF یا اسکریپت پیش‌بینی تقاضا با Python) قدم‌به‌قدم طرح فنی یا نمونه کد بنویسیم.

---

## ۷. وضعیت پیاده‌سازی (به‌روزرسانی)

| فیچر (مطابق سند) | وضعیت | محل دسترسی |
|-------------------|--------|-------------|
| **خواندن PDF و تشخیص خودکار محصول** (بخش ۲) | ✅ پیاده شده | ایمپورت ورود انبار → تب «PDF (تحلیل خودکار)» |
| **پیش‌بینی تقاضا** (۳.۲ – Exponential Smoothing) | ✅ پیاده شده | فیچرهای جدید → پیش‌بینی تقاضا |
| **هشدار موجودی کم / مقدار پیشنهادی سفارش** (۳.۲) | ✅ پیاده شده | فیچرهای جدید → هشدار موجودی هوشمند |
| **خوشه‌بندی مشتریان K-Means** (۳.۲) | ✅ پیاده شده | فیچرهای جدید → خوشه‌بندی مشتریان |
| **تشخیص ناهنجاری فروش** (۳.۲ – IQR) | ✅ پیاده شده | فیچرهای جدید → تشخیص ناهنجاری |
| گزارش مقایسه دو بازه، موجودی، انقضا، تراکنش‌ها (۳.۱) | ✅ از قبل موجود | گزارش انبار / گزارش فروش |
| آنالیز پارتو/ABC مشتریان، روند ماهانه، پرفروش‌ها (۳.۱) | ✅ از قبل موجود | گزارش فروش → تحلیل‌های فروش |
| تحلیل فصلی، تحلیل سود، وضعیت تحویل (۳.۱ / گزارش فروش) | ✅ از قبل موجود | گزارش فروش → تحلیل‌های فروش |
| پیش‌بینی با LSTM (۳.۳) | ✅ پیاده شده | فیچرهای جدید → پیش‌بینی تقاضا (LSTM)؛ نیاز به Python و TensorFlow |
| تشخیص ناهنجاری با Autoencoder (۳.۳) | ✅ پیاده شده | فیچرهای جدید → تشخیص ناهنجاری (Autoencoder)؛ نیاز به Python و TensorFlow یا scikit-learn |
