# الزامات داده برای فیچرهای هوشمند – راهنمای مهندسی داده و ML

این سند از دید **مهندس داده** و **مهندس یادگیری ماشین** توضیح می‌دهد برای اینکه فیچرهای جدید (پیش‌بینی، هشدار، خوشه‌بندی، ناهنجاری و LSTM/Autoencoder) **خوب و دقیق** کار کنند، سازمان باید چه داده‌ای با چه **حجم** و **کیفیت** در اختیار سیستم بگذارد و چه **رفتاری** (ثبت منظم، یکپارچگی، به‌روزرسانی) انتظار برود.

---

## ۱. اصل کلی: یادگیری ماشین به داده نیاز دارد

- **حجم:** هرچه تاریخچه طولانی‌تر و رکوردهای بیشتر (فروش، موجودی، مشتری) باشد، مدل الگوها را بهتر یاد می‌گیرد و پیش‌بینی/تحلیل دقیق‌تر می‌شود.
- **کیفیت:** داده **کامل** (فیلدهای ضروری خالی نباشند)، **یکدست** (مثلاً یک کد برای یک محصول)، **بدون نویز زیاد** (خطاهای ورود اصلاح شوند).
- **رفتار سازمان:** ثبت **مداوم و به‌موقع** (هر فروش، هر ورود/خروج انبار، هر تسویه) تا سیستم همیشه روی داده تازه یاد بگیرد و به‌روز بماند.

در ادامه برای **هر فیچر** دقیقاً گفته می‌شود از کدام جدول/فیلدها استفاده می‌شود، **حداقل و ایده‌آل حجم** چیست، و برای «پرفکت شدن» چه داده‌ای تکمیل یا اضافه شود.

در صفحه **فیچرهای جدید** علاوه بر تحلیل‌های پیش‌بینی و ناهنجاری، **چهار خلاصه** از بخش‌های حسابداری نمایش داده می‌شوند: **خلاصه فروش** (از `sales`)، **خلاصه انبار** (از `inventory`)، **خلاصه انبار بلزونا** (از `belzona_inventories`)، و **خلاصه تسویه فاکتور** (از `invoice_settlements`). هر چهار خلاصه از **کل داده** (بدون فیلتر تاریخ) محاسبه می‌شوند.

---

## ۲. فیچر به فیچر: داده مورد استفاده و حداقل/ایده‌آل حجم

### ۲.۱ پیش‌بینی تقاضا (Exponential Smoothing و LSTM)

**دادهٔ فعلی استفاده‌شده:**
- جدول **`sales`**: `document_date`, `product_code`, `product_name`, `main_quantity`, (اختیاری) `warehouse`.
- سیستم فروش را به‌ازای هر محصول در سطح **هفته** جمع می‌کند و روی این سری زمانی پیش‌بینی می‌زند.

**حداقل حجم برای «کار کردن معقول»:**
- برای **هر محصول** که می‌خواهید پیش‌بینی داشته باشید: حداقل **۸ تا ۱۲ نقطه زمانی (هفته)** با مقدار (یعنی در ۸ تا ۱۲ هفته اخیر حداقل یک فروش ثبت شده باشد).
- در کل دیتابیس: حداقل **چند صد رکورد فروش** در بازه ۳–۶ ماه تا توزیع هفتگی معنی‌دار شود.

**حجم ایده‌آل برای دقت خوب:**
- **۲۴ هفته (حدود ۶ ماه)** به بالا برای هر محصول پرفروش.
- **۵۲ هفته (۱ سال)** برای محصولات با فصل‌مندی یا نوسان زیاد.
- هرچه **تعداد هفته‌های پشت‌سرهم بدون شکاف** بیشتر باشد، LSTM و هموارسازی نمایی هر دو پایدارتر و دقیق‌تر می‌شوند.

**رفتار و کیفیت داده که سازمان باید رعایت کند:**
- **ثبت هر فروش با تاریخ واقعی** (`document_date` درست و یکدست).
- **یک کد ثابت برای هر محصول** (`product_code` یکسان با `inventory.inventory_code` در انبار).
- حذف یا اصلاح رکوردهای تست/خطا (مثلاً مقدار منفی یا تاریخ اشتباه) تا نویز کم شود.

**تکمیل برای پرفکت:**
- اگر **انبار** و **فروش** با یک کد یکسان لینک باشند، می‌توان پیش‌بینی را با «موجودی فعلی» ترکیب و پیشنهاد سفارش دقیق‌تر داد (همان دادهٔ فعلی کافی است؛ فقط یکپارچگی `product_code` مهم است).
- اختیاری: فیلد **دسته‌بندی محصول** یا **فصل** تا مدل برای محصولات کم‌داده از الگوی محصولات مشابه استفاده کند (در نسخه بعدی قابل اضافه‌سازی است).

---

### ۲.۲ هشدار موجودی هوشمند

**دادهٔ فعلی استفاده‌شده:**
- جدول **`inventory`**: `current_quantity`, `minimum_stock`, `inventory_code`.
- جدول **`inventory_entries`**: خروجی‌ها (ورود/خروج) برای محاسبه **مصرف روزانه**.
- جدول **`sales`**: فروش به‌ازای `product_code` برای همان بازه (تا مصرف از فروش هم استخراج شود).

**حداقل حجم:**
- برای هر کالایی که می‌خواهید «روز تا کمبود» و «پیشنهاد سفارش» داشته باشید: حداقل **۳۰ تا ۹۰ روز** تاریخچه خروج/فروش تا میانگین مصرف روزانه معقول باشد.
- اگر برای کالایی هیچ خروجی ثبت نشده، سیستم فقط بر اساس **موجودی و حداقل موجودی** هشدار می‌دهد (بدون «روز تا کمبود» دقیق).

**حجم ایده‌آل:**
- **۹۰ تا ۱۸۰ روز** خروج/فروش برای محاسبه مصرف میانگین و نوسان.
- پر بودن **حداقل موجودی** (`minimum_stock`) برای کالاهای مهم تا هشدار معنی‌دار باشد.

**رفتار سازمان:**
- **ثبت منظم ورود و خروج انبار** (ترجیحاً از همان ایمپورت یا فرم سیستم).
- **یکسان بودن** `product_code` در فروش با `inventory_code` در انبار تا مصرف از هر دو منبع درست جمع شود.

**تکمیل برای پرفکت:**
- تعیین **حداکثر موجودی** یا **نقطه سفارش مجدد (ROP)** برای کالاهای مهم (در صورت وجود فیلد در آینده، می‌توان پیشنهاد سفارش را به ROP گره زد).

---

### ۲.۳ خوشه‌بندی مشتریان (K-Means)

**دادهٔ فعلی استفاده‌شده:**
- جدول **`sales`**: `customer_name`, تعداد سفارش، مجموع مبلغ (`base_sales_amount`)، آخرین تاریخ خرید (`document_date`).
- از این‌ها سه ویژگی ساخته می‌شود: **مبلغ کل، تعداد سفارش، تازگی (recency)** و بعد K-Means اجرا می‌شود.

**حداقل حجم:**
- حداقل **۵۰ تا ۱۰۰ مشتری یکتا** با حداقل یک سفارش تا خوشه‌بندی معنی‌دار باشد.
- در کل **چند صد تا هزار رکورد فروش** در بازه انتخابی (مثلاً ۶ ماه یا ۱ سال).

**حجم ایده‌آل:**
- **۲۰۰ مشتری یکتا** به بالا و **حداقل ۱ سال** تاریخچه فروش تا الگوی A/B/C و فصل‌مندی مشتریان درست دیده شود.
- هرچه **نام مشتری یکدست** باشد (یک نام ثابت برای هر مشتری) نتیجه بهتر است.

**رفتار سازمان:**
- **ثبت یکسان نام مشتری** در همه فاکتورها (بدون نویز املایی یا چند نام برای یک مشتری).
- اختیاری: اگر جدول **مشتری** (مثلاً `clients`) دارید، بهتر است فروش با **شناسه مشتری** لینک شود تا نام عوض نشود.

**تکمیل برای پرفکت:**
- فیلد **دسته‌بندی مشتری** (B2B/B2C، منطقه، صنف) برای تفسیر بهتر خوشه‌ها.
- در آینده می‌توان مدل را با **تعداد محصولات مختلف خریداری‌شده** یا **میانگین مبلغ هر سفارش** غنی‌تر کرد (همان جدول `sales` کافی است اگر کامل پر شود).

---

### ۲.۴ تشخیص ناهنجاری فروش (IQR و Autoencoder)

**دادهٔ فعلی استفاده‌شده:**
- جدول **`sales`**: تجمع **روزانه** روی `document_date` → مجموع مبلغ و تعداد سفارش در هر روز.
- IQR و Autoencoder هر دو روی این سری روزانه کار می‌کنند (روزهای با مبلغ یا الگوی غیرعادی را پرچم می‌کنند).

**حداقل حجم:**
- حداقل **۳۰ روز** داده روزانه (با حداقل چند فروش در روز) تا میانگین و پراکندگی معقول باشد.
- در عمل **۹۰ روز** به بالا توصیه می‌شود تا روزهای تعطیل و نوسان هفتگی در نظر گرفته شوند.

**حجم ایده‌آل:**
- **۶ ماه تا ۱ سال** داده روزانه تا الگوی فصلی و تعطیلات در داده باشد و ناهنجاری واقعی از الگوی عادی جدا شود.
- روزهایی که **هیچ فروشی ثبت نشده** بهتر است به صورت صفر یا «بدون داده» یکدست باشند (نه حذف کامل).

**رفتار سازمان:**
- **ثبت روزانه و به‌موقع** فروش با تاریخ واقعی (نه انبوه‌ورودی در یک روز).
- حذف یا برچسب‌گذاری رکوردهای **تست/اصلاح** تا به‌عنوان ناهنجاری کاذب ظاهر نشوند.

**تکمیل برای پرفکت:**
- اختیاری: فیلد **نوع روز** (تعطیل/کاری) یا **منطقه** تا مدل بداند در چه زمینه‌ای ناهنجاری را ببیند (در نسخه بعدی قابل استفاده است).

---

### ۲.۵ استخراج از PDF و LSTM/Autoencoder (یادگیری عمیق)

- **PDF:** برای تشخیص خودکار محصول، **نام و کد کالا در سیستم یکدست** باشد و در صورت امکان چند **نمونه PDF واقعی با برچسب** (این PDF مربوط به کد X است) برای تست و تنظیم قوانین داشته باشید.
- **LSTM:** همان دادهٔ پیش‌بینی تقاضا؛ هرچه **سری زمانی طولانی‌تر** (۲۴+ هفته برای هر محصول) باشد، LSTM پایدارتر است.
- **Autoencoder:** همان دادهٔ ناهنجاری روزانه؛ **۹۰+ روز** با پر کردن روزهای بدون فروش (مثلاً با صفر) تا مدل الگوی عادی را بهتر یاد بگیرد.

---

## ۳. جدول خلاصه: حداقل و ایده‌آل حجم داده از سازمان

| فیچر | منبع داده | حداقل حجم (برای کار معقول) | حجم ایده‌آل (برای دقت خوب) |
|------|-----------|-----------------------------|-----------------------------|
| پیش‌بینی تقاضا | `sales` (تاریخ، محصول، مقدار) | ۸–۱۲ هفته داده به‌ازای هر محصول؛ جمعاً چند صد رکورد فروش | ۲۴–۵۲ هفته، ثبت مداوم |
| هشدار موجودی هوشمند | `inventory`, `inventory_entries`, `sales` | ۳۰–۹۰ روز خروج/فروش به‌ازای هر کالا؛ حداقل موجودی پر | ۹۰–۱۸۰ روز، ورود/خروج منظم |
| خوشه‌بندی مشتریان | `sales` (مشتری، مبلغ، تاریخ) | ۵۰–۱۰۰ مشتری یکتا، چند صد رکورد فروش | ۲۰۰+ مشتری، ۱ سال تاریخچه |
| تشخیص ناهنجاری | `sales` (تجمع روزانه) | ۳۰ روز روزانه | ۹۰ روز تا ۱ سال |
| LSTM | مثل پیش‌بینی تقاضا | ۱۰+ هفته به‌ازای هر محصول | ۲۴+ هفته |
| Autoencoder | مثل ناهنجاری | ۳۰+ روز | ۹۰+ روز |

---

## ۴. کیفیت داده: چه رفتاری از سازمان انتظار برود؟

برای اینکه سیستم **هوشمند و پایدار** عمل کند، از سازمان انتظار می‌رود:

1. **ثبت منظم و به‌موقع**
   - هر فروش با تاریخ واقعی در همان روز یا حداکثر با تأخیر کم.
   - هر ورود/خروج انبار در زمان وقوع.

2. **یکپارچگی شناسه‌ها**
   - یک **کد ثابت** برای هر محصول در انبار و فروش (`product_code` = `inventory_code`).
   - یک **نام/شناسه ثابت** برای هر مشتری در همه فاکتورها.

3. **پر بودن فیلدهای کلیدی**
   - فروش: `document_date`, `product_code` یا `product_name`, `customer_name`, `main_quantity`, `base_sales_amount`.
   - انبار: `minimum_stock` برای کالاهای مهم.
   - تسویه: در صورت استفاده از فیچرهای بعدی، `document_number`, `document_date`, `customer_name`, `base_net_amount`, `paid_amount`.

4. **پاکسازی نویز**
   - رکوردهای تست یا اشتباه حذف یا علامت‌دار شوند.
   - مقادیر منفی یا تاریخ‌های نامعتبر اصلاح شوند.

5. **تاریخچه طولانی**
   - هرچه داده قدیمی‌تر (با همان کیفیت) نگه‌داری شود، مدل برای پیش‌بینی و ناهنجاری بهتر یاد می‌گیرد؛ حداقل ۶ ماه تا ۱ سال برای بخش تحلیلی توصیه می‌شود.

---

## ۵. آیا همین داده‌های فعلی کافی است؟

- **برای «کار کردن» فیچرها:** بله؛ با همان جداول فعلی (`sales`, `inventory`, `inventory_entries`, `invoice_settlements`) و با رعایت **حداقل حجم** و **کیفیت** بالا، همه فیچرهای فعلی قابل استفاده‌اند.
- **برای «دقت خیلی خوب و پرفکت»:** باید:
  - **حجم** به سمت مقادیر **ایده‌آل** جدول بالا برود (تاریخچه ۶ ماه تا ۱ سال، مشتری و محصول یکدست).
  - **کیفیت** با ثبت منظم، یکپارچگی کد محصول و نام مشتری، و پاکسازی نویز رعایت شود.
  - در صورت نیاز برای تحلیل سود، وصول و تحویل، فیلدهای **تکمیلی** (مثل `unit_cost`, `due_date`, `paid_at`, `delivery_date`) طبق سند `AI_ML_ANALYSIS_ACCOUNTING.md` اضافه و پر شوند.

---

## ۶. چک‌لیست تحویل به تیم داده/سازمان

برای «پرفکت کردن» سیستم، از سازمان بخواهید:

- [ ] **فروش:** حداقل ۶ ماه تا ۱ سال تاریخچه با `document_date`, `product_code`, `product_name`, `customer_name`, `main_quantity`, `base_sales_amount` پر و یکدست.
- [ ] **انبار:** لیست کالاها با `inventory_code`, `inventory_name`, `minimum_stock`؛ و حداقل ۳–۶ ماه ورود/خروج در `inventory_entries` (یا از طریق ایمپورت).
- [ ] **یکسان بودن** `product_code` در فروش با `inventory_code` در انبار برای کالاهای مهم.
- [ ] **نام مشتری** یکدست در همه فاکتورها (یا لینک به جدول مشتری).
- [ ] **تسویه:** در صورت استفاده از گزارش تسویه، پر بودن `document_number`, `document_date`, `customer_name`, `base_net_amount`, `paid_amount`.
- [ ] **ثبت مداوم:** سیاست ثبت روزانه/هفتگی فروش و ورود/خروج انبار تا داده همیشه تازه باشد.

با این داده و رفتار، فیچرهای هوشمند فعلی پایدار و دقیق کار می‌کنند و در آینده با همان پایه داده می‌توان مدل‌های پیشرفته‌تر (تحلیل سود، پیش‌بینی وصول، و غیره) را اضافه کرد.
