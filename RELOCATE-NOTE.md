# اگر می‌خواهید مسیر docs تغییر کند

این پکیج با فرض `docs/ai/` تنظیم شده است.

اگر مسیر را عوض کردید:
1) `.cursorrules` را آپدیت کنید (فقط چند مسیر).
2) `MODE-SWITCH.md` و `OLLAMA-BOOTSTRAP.md` را آپدیت کنید.
3) برای Ollama: system prompt باید به مسیر جدید فایل start اشاره کند.
