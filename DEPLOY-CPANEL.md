# استقرار روی هاست cPanel و سازگاری فیچرها

## خلاصه: چه چیزی روی cPanel کار می‌کند؟

| فیچر | روی cPanel (هاست اشتراکی) | توضیح |
|------|---------------------------|--------|
| **کل نرم‌افزار (حسابداری، انبار، فروش، فاکتور، تسویه، گزارشات)** | ✅ کار می‌کند | فقط PHP و MySQL لازم است. |
| **ایمپورت Excel/CSV ورود انبار** | ✅ کار می‌کند | بدون وابستگی به پایتون. |
| **فیچرهای جدید – پیش‌بینی تقاضا (هموارسازی نمایی)** | ✅ کار می‌کند | محاسبه در PHP. |
| **فیچرهای جدید – هشدار موجودی هوشمند** | ✅ کار می‌کند | محاسبه در PHP. |
| **فیچرهای جدید – خوشه‌بندی مشتریان (K-Means)** | ✅ کار می‌کند | محاسبه در PHP. |
| **فیچرهای جدید – تشخیص ناهنجاری (IQR)** | ✅ کار می‌کند | محاسبه در PHP. |
| **ایمپورت از PDF (تحلیل خودکار)** | ❌ معمولاً نه | نیاز به پایتون و اجرای اسکریپت از PHP؛ روی هاست اشتراکی غالباً در دسترس یا مجاز نیست. |
| **پیش‌بینی LSTM** | ❌ معمولاً نه | وابسته به پایتون و TensorFlow. |
| **ناهنجاری با Autoencoder** | ❌ معمولاً نه | وابسته به پایتون و TensorFlow/sklearn. |

بنابراین **دمو روی cPanel کاملاً قابل استفاده است**: همه بخش‌ها و اکثر فیچرهای هوشمند (همان‌هایی که در PHP پیاده شده‌اند) کار می‌کنند. فقط سه قابلیت وابسته به پایتون (PDF، LSTM، Autoencoder) روی هاست اشتراکی معمولاً غیرفعال یا غیرقابل اجرا هستند.

---

## تنظیم برای cPanel: جلوگیری از خطا برای فیچرهای پایتون

روی هاست cPanel در فایل **`.env`** این مقدار را قرار دهید تا سیستم اصلاً سعی نکند پایتون را صدا بزند و خطای exec ندهد:

```env
ENABLE_PYTHON_ML=false
```

با این کار:
- **تب PDF** در صفحه ایمپورت ورود انبار یک پیام «غیرفعال در این سرور» نشان می‌دهد و فرم ارسال PDF نمایش داده نمی‌شود.
- **پیش‌بینی LSTM** همان خروجی پیش‌بینی PHP (هموارسازی نمایی) را برمی‌گرداند و در ستون «روش» عبارت «غیرفعال در این سرور» دیده می‌شود.
- **ناهنجاری Autoencoder** فقط نتیجه IQR را نشان می‌دهد و روش «غیرفعال در این سرور» نمایش داده می‌شود.

هیچ خطای PHP یا تماس با پایتون انجام نمی‌شود و دمو پایدار می‌ماند.

---

## تنظیم برای لاراگون (یا سروری که پایتون دارید)

در **`.env`** روی لاراگون:

```env
ENABLE_PYTHON_ML=true
PYTHON_PATH=C:\laragon\bin\python\python-3.13\python.exe
```

دقت کنید مسیر باید به فایل **اجرایی** پایتون (`python.exe`) ختم شود، نه فقط به پوشه. اگر پایتون در PATH است و با دستور `python` اجرا می‌شود، می‌توانید فقط بگذارید:

```env
PYTHON_PATH=python
```

---

## جمع‌بندی

- **لاراگون:** با نصب پایتون و پکیج‌های `tools/requirements-ml.txt` و تنظیم `PYTHON_PATH` در `.env`، همه فیچرها از جمله PDF، LSTM و Autoencoder قابل استفاده‌اند.
- **هاست cPanel (دمو):** با قرار دادن `ENABLE_PYTHON_ML=false` در `.env`، کل نرم‌افزار و تمام فیچرهای مبتنی بر PHP بدون خطا و به‌صورت کاربردی در دسترس هستند؛ فقط سه فیچر وابسته به پایتون در آن محیط غیرفعال می‌مانند.
