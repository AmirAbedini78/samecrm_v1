# راهنمای فیچرهای هوشمند (فیچر جدید)

این سند بر اساس **داده‌های واقعی دیتابیس** و **منطق کد** نوشته شده است. ابتدا خلاصهٔ داده‌ها، سپس توضیح هر فیچر، منبع داده، الگوریتم و معنای خروجی‌ها.

---

## بخش ۱: خلاصهٔ داده‌های دیتابیس (بر اساس آخرین کوئری)

| جدول | تعداد رکورد | توضیح کوتاه |
|------|-------------|--------------|
| **sales** (فروش) | ۱٬۸۸۴ | تاریخ‌ها مخلوط شمسی (۱۴۰۳، ۱۴۰۴) و میلادی (۲۰۲۵). ۱٬۰۶ مشتری یکتا، ۱٬۳۴۶ محصول یکتا. |
| **inventory** (انبار) | ۲٬۰۴۲ | همه فعال. حداقل موجودی (minimum_stock) در نمونه‌ها ۰ است. |
| **inventory_entries** | ۰ | ورود/خروج انبار در این جدول ثبت نشده. |
| **belzona_inventories** (انبار بلزونا) | ۳٬۲۵۸ | تاریخ‌ها میلادی (۲۰۰۸ تا ۲۰۲۵). ۳۶ محصول یکتا. مجموع خروجی و ورود موجود. |
| **invoice_settlements** (تسویه فاکتور) | ۲۰۸ | تاریخ شمسی (مثل ۱۴۰۴/۰۴/۱۷). مشتری و مبلغ و مانده موجود. |

- **فروش:** قدیمی‌ترین تاریخ `1403-01-21`، جدیدترین `2025-10-28`. توزیع: ۱٬۱۰۵ رکورد سال ۱۴۰۳، ۷۷۷ رکورد ۱۴۰۴، ۲ رکورد ۲۰۲۵.
- **انبار:** مصرف روزانه از طریق **فروش** (با تطابق کد محصول) و **خروج انبار بلزونا** (با تطابق نام/کد) محاسبه می‌شود؛ چون `inventory_entries` خالی است.
- برای دیدن گزارش به‌روز از پوشهٔ `application` اجرا کنید:  
  `php artisan smart-features:db-report`  
  خروجی در `application/storage/app/smart_features_db_report.txt` ذخیره می‌شود.  
  با گزینه `--output=مسیر/فایل.txt` می‌توانید مسیر خروجی را عوض کنید.

---

## بخش ۲: فیلتر بازهٔ زمانی

- در صفحه «فیچرهای جدید» می‌توانید **از تاریخ**، **تا تاریخ** یا **بازه بر اساس روز** (مثلاً ۹۰ روز، یک سال) انتخاب کنید.
- اگر **«همهٔ داده‌ها»** (۳۶۵۰ روز) را انتخاب کنید، **هیچ فیلتر زمانی** روی دیتابیس اعمال نمی‌شود و تمام رکوردها استفاده می‌شوند.
- اگر بازهٔ عددی (مثلاً ۹۰ یا ۱۸۰ روز) انتخاب شود، فقط رکوردهایی که **تاریخ سند** در آن بازه باشد در نظر گرفته می‌شوند. توجه: در جدول فروش بعضی تاریخ‌ها **شمسی** (۱۴۰۳-…) هستند؛ مقایسه با تاریخ میلادی ممکن است بخشی از داده را حذف کند. برای دیدن بیشترین داده، «همهٔ داده‌ها» را امتحان کنید.

---

## بخش ۳: توضیح هر فیچر، منبع داده و معنای خروجی

---

### ۳.۱ پیش‌بینی تقاضا (Exponential Smoothing)

**منبع داده:**
- اول از جدول **sales**: به ازای هر ترکیب `(product_code, product_name, document_date)` جمع مقدار فروش (`main_quantity`) و جمع مبلغ (`base_sales_amount`) گرفته می‌شود.
- اگر هیچ رکورد فروش در بازه نباشد، از جدول **belzona_inventories** استفاده می‌شود: به ازای هر `(product_name, date)` جمع فیلد **output** به عنوان «مقدار» تقاضا.

**منطق:**
1. داده‌ها به **هفته** (سال-شماره هفته) گروه‌بندی می‌شوند.
2. برای هر محصول، دنبالهٔ مقدار فروش (یا خروج بلزونا) در هفته‌ها ساخته می‌شود.
3. **هموارسازی نمایی** (Exponential Smoothing) با ضریب آلفا ۰.۳ روی این مقادیر اعمال می‌شود و یک عدد به عنوان پیش‌بینی دورهٔ بعد به دست می‌آید.
4. **روند** = آخرین مقدار هفتگی منهای اولین مقدار (مثبت = روند صعودی، منفی = نزولی).

**ستون‌های خروجی و معنای آن‌ها:**

| ستون | معنا |
|------|------|
| **کد کالا** | `product_code` از فروش یا خالی در صورت منبع بلزونا. |
| **نام کالا** | نام محصول. |
| **میانگین هفتگی** | میانگین مقدار فروش/خروج در هر هفته در بازهٔ انتخاب‌شده. |
| **پیش‌بینی دوره بعد** | مقدار پیش‌بینی‌شده برای دوره (هفته) بعد با روش هموارسازی نمایی. برای برنامه‌ریزی سفارش/تولید مفید است. |
| **روند** | تفاوت آخرین هفته با اولین هفته؛ مثبت یعنی تقاضا در حال افزایش، منفی یعنی کاهش. |

جدول حداکثر ۳۰ محصول را نشان می‌دهد و بر اساس **بیشترین پیش‌بینی دوره بعد** مرتب شده است.

---

### ۳.۲ پیش‌بینی تقاضا (LSTM – یادگیری عمیق)

**منبع داده:** همان منبع پیش‌بینی تقاضا (فروش یا بلزونا)، با گروه‌بندی هفتگی.

**منطق:**
- همان خروجی **پیش‌بینی تقاضا (Exponential Smoothing)** به عنوان پایه برگردانده می‌شود.
- اگر **Python و اسکریپت LSTM** فعال باشد و برای یک محصول حداقل **۱۰ نقطهٔ زمانی** (هفته) وجود داشته باشد، یک مدل LSTM روی سری زمانی آن محصول اجرا می‌شود و چند **قدم پیش‌بینی** (مثلاً ۴ قدم) به خروجی اضافه می‌شود.
- اگر Python غیرفعال یا داده کافی نباشد، فقط همان پیش‌بینی هموارسازی نمایی و ستون‌های LSTM خالی یا با توضیح «غیرفعال در این سرور» نمایش داده می‌شود.

**ستون‌های اضافه و معنای آن‌ها:**

| ستون | معنا |
|------|------|
| **پیش‌بینی دوره بعد** | همان خروجی هموارسازی نمایی. |
| **LSTM predictions** | اعداد پیش‌بینی برای چند دورهٔ بعد (خروجی شبکه LSTM). |
| **lstm_method** | روش استفاده‌شده (مثلاً lstm) یا پیام غیرفعال بودن. |

---

### ۳.۳ هشدار موجودی هوشمند

**منبع داده:**
- جدول **inventory**: فقط آیتم‌های با وضعیت فعال (`inventory_status = active`). فیلدهای استفاده‌شده: `inventory_code`, `inventory_name`, `current_quantity`, `minimum_stock`, `main_unit`.
- **مصرف روزانه** برای هر قلم انبار از سه جا محاسبه می‌شود:
  1. **inventory_entries**: جمع مقدار خروجی (نوع خروجی/output/OUT/خروج) در بازهٔ زمانی.
  2. **sales**: جمع `main_quantity` ردیف‌هایی که `product_code` آن‌ها با `inventory_code` این قلم یکی باشد.
  3. **belzona_inventories**: جمع فیلد **output** ردیف‌هایی که `product_name` با `inventory_name` یا `inventory_code` یکی باشد.

بازهٔ محاسبهٔ مصرف با پارامتر **days_back** (پیش‌فرض تا ۳۶۵۰ روز) تعیین می‌شود.

**منطق:**
- برای هر قلم انبار:  
  **میانگین مصرف روزانه** = مجموع خروج (از سه منبع بالا) ÷ تعداد روزهای بازه.
- **روز تا رسیدن به حداقل موجودی** = اگر مصرف روزانه > ۰ باشد: `(current_quantity - minimum_stock) / مصرف_روزانه`؛ وگرنه عدد بزرگی (مثلاً ۹۹۹) نشان داده می‌شود.
- **مقدار پیشنهادی سفارش** = اگر موجودی فعلی کمتر از حداقل باشد: `minimum_stock - current_quantity`؛ وگرنه ۰.
- اگر **minimum_stock** صفر باشد، «روز تا حداقل» و «مقدار پیشنهادی سفارش» صفر یا بدون هشدار در نظر گرفته می‌شوند.

**ستون‌های خروجی و معنای آن‌ها:**

| ستون | معنا |
|------|------|
| **کد انبار / نام انبار** | شناسه و نام قلم در انبار. |
| **موجودی فعلی** | مقدار فعلی در انبار. |
| **حداقل موجودی** | حدی که برای هشدار کمبود استفاده می‌شود. |
| **میانگین مصرف روزانه** | میانگین خروج روزانه در بازه (از فروش + انبار + بلزونا). |
| **روز تا رسیدن به حداقل** | تقریباً چند روز دیگر با همین مصرف، موجودی به حداقل می‌رسد. |
| **مقدار پیشنهادی سفارش** | چقدر سفارش/تولید پیشنهاد می‌شود تا به حداقل برسید. |
| **واحد** | واحد اصلی قلم. |
| **کم** | اگر موجودی فعلی ≤ حداقل باشد، به صورت هشدار (مثلاً badge قرمز) نمایش داده می‌شود. |

در دیتابیس فعلی، همهٔ نمونه‌های انبار `minimum_stock = 0` دارند؛ در نتیجه برای آن‌ها «مقدار پیشنهادی سفارش» و هشدار کم بر اساس حداقل موجودی صفر می‌شود. با پر کردن **حداقل موجودی** برای اقلام مهم، خروجی این بخش کاربردی‌تر می‌شود.

---

### ۳.۴ خوشه‌بندی مشتریان (Customer Clustering)

**منبع داده:**
- اول از **sales**: برای هر `customer_name` (غیر خالی) جمع‌آوری می‌شود: تعداد سفارش، مجموع مبلغ (`base_sales_amount`)، آخرین تاریخ (`document_date`).
- اگر در بازهٔ انتخاب‌شده هیچ مشتری از فروش نیامد، از **invoice_settlements** استفاده می‌شود: برای هر `customer_name` تعداد سند، مجموع `base_net_amount` و آخرین `document_date`.

**منطق:**
- برای هر مشتری سه شاخص نرمال‌شده محاسبه می‌شود:
  1. **مبلغ کل** (نرمال روی بیشترین مبلغ).
  2. **تعداد سفارش/سند** (نرمال روی بیشترین تعداد).
  3. **تازگی (Recency)**: هر چه آخرین خرید/سند نزدیک‌تر باشد عدد بزرگ‌تر (نرمال بر اساس حداکثر ۳۶۵ روز).
- الگوریتم **K-Means** با ۳ خوشه روی این سه بعد اجرا می‌شود و هر مشتری به یکی از سه بخش اختصاص داده می‌شود.
- برچسب‌های پیش‌فرض: **A (باارزش)**، **B (متوسط)**، **C (کم‌تعامل)**. ترتیب عددی خوشه‌ها ممکن است با این برچسب‌ها متناظر نباشد؛ معمولاً خوشه‌ای که میانگین مبلغ/تعدادش بیشتر است «باارزش‌تر» تفسیر می‌شود.

**خروجی و معنای آن:**

- **segments**: لیست ۳ خوشه؛ هر خوشه شامل: برچسب، تعداد مشتری، و لیست (مثلاً تا ۵۰ نفر) از مشتریان آن خوشه با فیلدهای:
  - **customer_name**
  - **order_count**: تعداد سفارش/سند
  - **total_amount**: مجموع مبلغ
  - **recency_days**: چند روز از آخرین خرید/سند گذشته
  - **segment_label**: نام همان خوشه (A/B/C یا «بخش …»)

**کاربرد:** شناسایی مشتریان باارزش برای تمرکز فروش/پشتیبانی، و مشتریان کم‌تعامل برای کمپین یا بازبینی.

---

### ۳.۵ تشخیص ناهنجاری (Anomaly Detection – IQR و Z-Score)

**منبع داده:**
- اول از **sales**: برای هر **روز** (DATE(document_date)) جمع تعداد سفارش و مجموع `base_sales_amount` به عنوان «مبلغ کل روز».
- اگر در بازه دادهٔ روزانه از فروش نیامد، از **belzona_inventories** استفاده می‌شود: برای هر **روز** (DATE(date)) جمع فیلد **output** به عنوان «مبلغ کل روز» و تعداد رکورد به عنوان «تعداد».

**منطق:**
- روی سری «مبلغ کل هر روز» آمار محاسبه می‌شود: میانگین، انحراف معیار، چارک اول (Q1) و سوم (Q3)، و **IQR** = Q3 − Q1.
- بازهٔ «عادی» با روش IQR تعریف می‌شود:  
  پایین = Q1 − ۱.۵×IQR ، بالا = Q3 + ۱.۵×IQR.
- هر روز که مبلغش **کمتر از پایین** یا **بیشتر از بالا** باشد، یا **Z-Score** آن (فاصله از میانگین بر حسب انحراف معیار) بیشتر از ۲ باشد، به عنوان **ناهنجاری** علامت می‌خورد.
- دلیل ناهنجاری به صورت متنی تعیین می‌شود: «فروش بسیار بالا»، «فروش بسیار پایین»، یا «انحراف از میانگین».

**خروجی و معنای آن:**

- **anomalies**: لیست روزهای ناهنجار با فیلدهای:
  - **day**: تاریخ
  - **order_count**: تعداد سفارش/تراکنش آن روز
  - **total_amount**: مبلغ آن روز
  - **reason**: توضیح کوتاه (بسیار بالا / بسیار پایین / انحراف از میانگین)
- **stats**: میانگین، انحراف معیار، Q1، Q3، کران پایین و بالا (برای تحلیل دستی).
- **days**: لیست همهٔ روزها با مبلغ، تعداد، z_score و پرچم is_anomaly.

**کاربرد:** پیدا کردن روزهای غیرعادی (جهش یا افت فروش/خروج) برای بررسی علت (تخفیف، رویداد، خطای ثبت و غیره).

---

### ۳.۶ تشخیص ناهنجاری با Autoencoder (یادگیری عمیق)

**منبع داده:** همان دادهٔ روزانهٔ فیچر «تشخیص ناهنجاری» (فروش یا بلزونا).

**منطق:**
- خروجی **IQR/Z-Score** همان‌طور که در بالا توضیح داده شد محاسبه و برگردانده می‌شود.
- اگر **Python و اسکریپت Autoencoder** فعال باشد و تعداد روزها حداقل ۱۰ باشد، یک مدل **Autoencoder** روی سری مقادیر روزانه آموزش دیده می‌شود. روزهایی که خطای بازسازی (reconstruction error) آن‌ها از آستانه بالاتر باشد به عنوان **ناهنجاری Autoencoder** به خروجی اضافه می‌شوند.
- در خروجی هم ناهنجاری‌های IQR و هم (در صورت اجرا) ناهنجاری‌های Autoencoder نمایش داده می‌شوند.

**معنای خروجی:** همان‌طور که در بخش ۳.۵ گفته شد؛ به علاوه بخش **autoencoder_anomalies** و **autoencoder_method** که نشان می‌دهد آیا از مدل Autoencoder استفاده شده یا خیر.

---

### ۳.۷ خلاصه فروش

**منبع داده:** فقط جدول **sales** (فروش). بدون فیلتر تاریخ؛ همهٔ رکوردها.

**خروجی و معنای آن:**

- **record_count**: تعداد کل رکوردهای فروش.
- **total_amount**: مجموع مبلغ فروش (base_sales_amount).
- **total_quantity**: مجموع مقدار فروش (main_quantity).
- **min_date** / **max_date**: قدیمی‌ترین و جدیدترین تاریخ سند در جدول فروش.
- **unique_products**: تعداد محصول یکتا (بر اساس product_name).
- **unique_customers**: تعداد مشتری یکتا (بر اساس customer_name).
- **top_by_amount**: چند محصول با بیشترین مجموع مبلغ فروش (کد، نام، مجموع مبلغ، مجموع مقدار).
- **top_customers**: چند مشتری با بیشترین مجموع مبلغ خرید (نام، مجموع مبلغ، تعداد سند).

**کاربرد:** دید سریع از حجم فروش، بازهٔ زمانی داده، و مهم‌ترین محصولات و مشتریان.

---

### ۳.۸ خلاصه انبار

**منبع داده:** فقط جدول **inventory** (انبار). بدون فیلتر تاریخ؛ فقط اقلام فعال.

**خروجی و معنای آن:**

- **record_count**: تعداد کل اقلام انبار.
- **active_count**: تعداد اقلام با وضعیت فعال (inventory_status = active).
- **with_minimum_stock**: تعداد اقلامی که حداقل موجودی (minimum_stock) بزرگ‌تر از صفر دارند.
- **total_quantity**: مجموع موجودی فعلی (current_quantity) همهٔ اقلام فعال.
- **low_stock_count**: تعداد اقلامی که موجودی فعلی آن‌ها کمتر یا مساوی حداقل موجودی است (نیاز به توجه).
- **zero_stock_count**: تعداد اقلام فعال با موجودی صفر.
- **top_by_quantity**: چند قلم با بیشترین موجودی فعلی (کد، نام، موجودی، حداقل، واحد).

**کاربرد:** دید سریع از وضعیت انبار، تعداد اقلام زیر حد یا صفر، و اقلام با بیشترین موجودی.

---

### ۳.۹ خلاصه تسویه فاکتور

**منبع داده:** فقط جدول **invoice_settlements**. بدون فیلتر تاریخ؛ همهٔ رکوردها.

**خروجی و معنای آن:**

- **total_balance**: مجموع مانده (balance_amount) همهٔ اسناد.
- **total_net**: مجموع مبلغ خالص (base_net_amount).
- **total_paid**: مجموع مبلغ پرداخت‌شده (paid_amount).
- **record_count**: تعداد رکورد تسویه.
- **top_by_balance**: چند مشتری با بیشترین مانده (نام مشتری، مانده، تعداد سند).

**کاربرد:** دید سریع از وضعیت مطالبات و مشتریانی که بیشترین مانده را دارند.

---

### ۳.۱۰ خلاصه انبار بلزونا

**منبع داده:** فقط جدول **belzona_inventories**. بدون فیلتر تاریخ.

**خروجی و معنای آن:**

- **record_count**: تعداد رکورد.
- **top_by_output**: چند محصول با بیشترین مجموع خروج (output) به همراه مجموع ورود (input).

**کاربرد:** دیدن محصولات پرفروش/پرخرج در انبار بلزونا.

---

## بخش ۴: جمع‌بندی جریان داده

در صفحه «فیچرهای جدید» ابتدا **چهار خلاصه** از بخش‌های حسابداری نمایش داده می‌شوند (همه از کل داده، بدون فیلتر تاریخ):

- **خلاصه فروش** ← جدول `sales`
- **خلاصه انبار** ← جدول `inventory`
- **خلاصه انبار بلزونا** ← جدول `belzona_inventories`
- **خلاصه تسویه فاکتور** ← جدول `invoice_settlements`

سپس فیچرهای تحلیلی (با امکان فیلتر بازهٔ زمانی):

```
فیچر جدید (صفحه)
    │
    ├─ خلاصه فروش ────────► sales (کل)
    ├─ خلاصه انبار ────────► inventory (کل، فقط فعال)
    ├─ خلاصه انبار بلزونا ► belzona_inventories (کل)
    ├─ خلاصه تسویه ───────► invoice_settlements (کل)
    │
    ├─ فیلتر: از تاریخ / تا تاریخ / روز (یا «همهٔ داده‌ها» = بدون فیلتر)
    │
    ├─ پیش‌بینی تقاضا ─────► sales (یا بلزونا) → گروه هفتگی → هموارسازی نمایی → جدول خروجی
    ├─ پیش‌بینی LSTM ─────► همان + در صورت امکان اسکریپت پایتون LSTM
    ├─ هشدار موجودی ─────► inventory + مصرف از (inventory_entries + sales + بلزونا)
    ├─ خوشه‌بندی مشتری ──► sales (یا invoice_settlements) → K-Means سه خوشه
    ├─ ناهنجاری ─────────► sales روزانه (یا بلزونا روزانه) → IQR + Z-Score
    └─ ناهنجاری Autoencoder ► همان + در صورت امکان اسکریپت پایتون
```

با این راهنما می‌توانید بر اساس **دادهٔ واقعی** خود هر فیچر را دقیق‌تر بررسی کنید و بدانید هر عدد در خروجی از کجا آمده و به چه معناست. برای به‌روزرسانی گزارش خام دیتابیس همیشه می‌توانید دستور `php artisan smart-features:db-report` را اجرا کنید.
